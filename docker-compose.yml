version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: sh
    stdin_open: true
    tty: true
    volumes:
      - ./:/var/app
    environment:
      - ENVIRONMENT=development
